const Home = () => {
    return (
        <div>
            <h1>Redirect to discord's login page</h1>
            <a href="/api/auth/redirect">Login</a>
        </div>
    );
};

export default Home;
